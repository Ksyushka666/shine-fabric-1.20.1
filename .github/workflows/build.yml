name: Build Shine

on:
  push:
    branches: [ "**" ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - name: Make Gradle executable
        run: chmod +x gradlew
      - name: Build and verify
        run: ./gradlew clean check build --no-daemon
      - name: Generate checksums
        run: sha256sum build/libs/*.jar > SHA256SUMS.txt
      - name: Upload verified artifacts
        uses: actions/upload-artifact@v4
        with:
          name: shine-fabric-1.20.1-${{ github.sha }}
          path: |
            build/libs/*.jar
            SHA256SUMS.txt
            PORT_STATUS.md
          if-no-files-found: error
      - name: Publish GitHub Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version=$(grep '^mod_version=' gradle.properties | cut -d= -f2)
          artifact="shine-fabric-1.20.1-${version}"
          tag="v${version}-build-${GITHUB_RUN_NUMBER}"
          gh release create "$tag" build/libs/${artifact}.jar build/libs/${artifact}-sources.jar SHA256SUMS.txt \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "Shine Fabric 1.20.1 ${version}" \
            --notes "Automated release created after a successful clean check build. Commit: ${GITHUB_SHA}." \
            --latest
